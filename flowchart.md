# フローチャート

```mermaid
graph TD
    Step1([1. アプリ起動またはブラウザの更新]) --> Step2[2. UI基本設定]
    Step2 --> Step3{"3. メモリ領域にデータ(df)が存在するか？"}
    
    Step3 -- Yes --> Step5[5. 既存のメモリ領域のデータを維持]
    Step3 -- No --> Step4{"4. ローカルにdata.csvが存在するか？"}
    
    Step4 -- Yes --> Step6["6. CSVから履歴・設定を読み込み、メモリ領域へ格納"]
    Step4 -- No --> Step7["7. 空のdf・初期アカウント・基本リストを生成し、メモリ領域へ格納"]
    
    Step6 --> Step8["8. 残高の自動計算"]
    Step7 --> Step8
    Step5 --> Step8
    
    Step8 --> Step9["9. メイン画面への描画およびサイドバー描画"]
    Step9 --> Step10["10. 新規データの入力フォーム処理"]
    Step10 --> Step11["11. タブ1: 支出の分析の描画"]
    Step11 --> Step12["12. タブ2: 履歴の一覧の描画"]
    Step12 --> Step13["13. タブ3: 設定・データ管理の描画"]
    
    Step10 -.->|ボタン押下時のrerun| Step2